# Desktop Gravity（桌面重力系统）开发规格书

> 本文档是交给 Codex 的实现蓝图。请先完整阅读，再检查当前仓库状态，随后按“实施顺序”逐步实现。除非遇到会改变产品方向的重大歧义，否则不要只给建议或伪代码：直接创建项目、编写代码、运行可执行的检查，并在每个阶段汇报已完成内容、验证结果、已知限制和下一步。

## 1. 项目概述

### 1.1 项目名称

- 英文名：Desktop Gravity
- 中文名：桌面重力系统
- 建议仓库名：`desktop-gravity`

### 1.2 一句话介绍

为 Windows 桌面窗口加入可调节的重力、落地和弹跳效果，让普通应用窗口像现实中的物体一样下坠。

### 1.3 产品目标

开发一个适合开源发布和视频演示的 Windows 桌面程序。用户选择一个或多个普通窗口并开启重力后，窗口会向当前显示器的工作区底部下落、碰撞边界并弹跳。程序必须始终可控、可恢复，不得影响文件内容或关闭其他程序。

### 1.4 核心体验

1. 用户启动 Desktop Gravity。
2. 主界面列出当前可控制的普通顶层窗口。
3. 用户勾选一个或多个窗口。
4. 用户点击“启用重力”。
5. 被选中的窗口开始自然下落并在任务栏上方弹跳。
6. 用户拖起窗口并松手后，可以重新下落；若实现投掷功能，窗口保留释放瞬间的速度。
7. 用户按紧急快捷键或点击“停止”，所有物理效果立即结束。

### 1.5 目标平台与技术栈

- 目标系统：Windows 10 22H2、Windows 11
- 架构：优先支持 x64
- 语言：C#
- 框架：.NET 8
- UI：WPF
- Windows 互操作：P/Invoke 调用 Win32 API
- 测试：xUnit
- 配置持久化：本地 JSON
- 日志：本地文本日志，默认不记录窗口标题全文，不上传任何数据

## 2. 产品原则

实现过程中按以下优先级取舍：

1. 可随时停止，高于视觉效果。
2. 不丢失窗口，高于物理真实性。
3. 稳定流畅，高于复杂碰撞。
4. 默认权限运行，高于控制所有窗口。
5. 本地处理和隐私，高于数据分析。
6. MVP 可演示，高于一次性完成全部设想。

## 3. MVP 范围

### 3.1 MVP 必须实现

#### A. 主界面

- 显示程序名称和当前状态：`Stopped`、`Running`、`Paused`。
- 显示可控制窗口列表。
- 每项至少显示：窗口标题、所属进程名、是否已选中。
- 提供“刷新窗口列表”按钮。
- 提供“全选可用窗口”和“全部取消”操作。
- 提供“启动重力”“暂停”“停止并恢复”按钮。
- 提供重力强度、弹性系数和刷新率设置。
- 显示全局紧急停止快捷键。

#### B. 窗口发现与筛选

- 使用 `EnumWindows` 枚举顶层窗口。
- 使用 `IsWindowVisible`、`GetWindowRect` 等信息过滤窗口。
- 仅显示有有效尺寸且适合移动的普通可见顶层窗口。
- 排除 Desktop Gravity 自身所有窗口。
- 排除桌面、任务栏、开始菜单、Shell、悬浮提示、不可见窗口和最小化窗口。
- 排除明显无标题、尺寸异常、位置异常或无法读取的窗口。
- 无法控制的窗口应跳过并记录简短诊断，不得导致程序崩溃。

#### C. 重力模拟

- 每个受控窗口维护独立物理状态：位置、垂直速度、是否落地、最后更新时间。
- 使用基于时间步长的运动计算，不得假设循环每次固定耗时。
- 基本公式：`velocityY += gravity * deltaTime`，`y += velocityY * deltaTime`。
- 使用当前窗口所在显示器的工作区作为边界，不能覆盖任务栏。
- 窗口触底时修正位置并按弹性系数反转垂直速度。
- 速度低于阈值时停止微小弹跳，使窗口稳定落地。
- 设置最大速度，避免暂停或卡顿后窗口瞬移。
- MVP 只要求垂直重力，不要求窗口之间互相碰撞。

#### D. 用户拖动处理

- 检测用户正在移动或调整窗口大小时，暂停该窗口的物理更新。
- 用户松开后重新读取窗口位置，再继续下落。
- MVP 不强制实现“投掷速度”；如果检测不可靠，释放后以零初速度下落。
- 不得与用户争抢窗口位置，避免拖动时抖动。

#### E. 安全停止与恢复

- 注册全局快捷键，默认 `Ctrl + Shift + G`，用于立即停止全部物理效果。
- 主界面的停止按钮具有相同效果。
- 停止后清除全部速度和活动状态。
- “停止并恢复”将仍然存在的受控窗口恢复到启用重力前的位置。
- 恢复位置时必须限制在当前可用显示器范围内，不能把窗口恢复到屏幕外。
- 程序异常退出、正常退出或托盘退出时尽最大努力停止更新；不得继续留下后台控制逻辑。
- 禁止拦截任务管理器、Windows 键、Alt+F4 或系统恢复工具。

#### F. 本地配置

- 保存重力、弹性、帧率和快捷键设置。
- 配置文件放在用户本地应用数据目录下的独立文件夹中。
- 配置损坏时回退到安全默认值，不得启动失败。
- 不保存窗口标题历史，除非后续明确加入且用户主动同意。

### 3.2 MVP 明确不做

- 不关闭、终止或挂起其他进程。
- 不注入 DLL，不修改其他进程内存。
- 不修改注册表，不设置开机启动。
- 不申请管理员权限来控制高权限窗口。
- 不移动 UAC、安全桌面、登录界面或系统关键窗口。
- 不实现窗口旋转、碎裂、透明变形或三维物理。
- 不实现窗口之间互相碰撞。
- 不实现联网、遥测、账号或云端同步。
- 不自动对所有窗口启用重力。

## 4. 默认参数

| 参数 | 默认值 | 允许范围 | 说明 |
| --- | ---: | ---: | --- |
| 重力加速度 | 1800 px/s² | 100–5000 | 向下为正 |
| 弹性系数 | 0.55 | 0–0.95 | 0 表示不反弹 |
| 目标刷新率 | 60 FPS | 15、30、60 | 性能不足时允许自动降级 |
| 最大垂直速度 | 2500 px/s | 500–5000 | 防止窗口瞬移 |
| 静止速度阈值 | 35 px/s | 5–100 | 低于该速度时落地静止 |
| 紧急停止快捷键 | Ctrl+Shift+G | 可配置 | 冲突时提示用户 |

默认值集中定义，不要散落在 UI、物理引擎和配置代码中。

## 5. 窗口资格规则

一个窗口只有同时满足以下条件时才可出现在可选列表中：

- 句柄有效且窗口可见。
- 是顶层窗口，不是子窗口。
- 未最小化。
- 宽高均大于合理阈值，例如 120×80 像素。
- 与至少一个显示器工作区相交。
- 不是本程序窗口。
- 不是桌面、任务栏、开始菜单或已知系统 Shell 窗口。
- 不属于明确的排除进程或排除类名。
- 当前进程有能力读取其位置并尝试移动。

筛选逻辑必须集中在独立服务中，并为纯判断部分编写单元测试。排除列表以安全默认值开始，后续可扩展；不要为了覆盖所有特殊窗口写大量无法验证的硬编码。

## 6. 多显示器与边界规则

- 每次开始控制窗口时，根据窗口中心点确定其当前显示器。
- 边界使用该显示器的工作区，而不是完整屏幕区域。
- 窗口下落过程中，如果超过一半面积进入另一显示器，则更新所属显示器。
- 如果显示器断开，立即把相关窗口限制到剩余主显示器的可见范围。
- 无论发生何种异常，至少保留标题栏的一部分可见。
- 不要求 MVP 实现跨显示器水平投掷，但不得因此产生越界或崩溃。

## 7. UI 设计

### 7.1 主窗口布局

建议使用简洁的单窗口布局：

1. 顶部：项目名称、运行状态、紧急快捷键提示。
2. 中部左侧：可控制窗口列表和复选框。
3. 中部右侧：重力、弹性、刷新率设置。
4. 底部：刷新、启动、暂停、停止并恢复。
5. 最底部：诊断信息，例如“控制 3 个窗口，跳过 2 个窗口”。

### 7.2 状态约束

- `Stopped`：可以刷新列表、修改设置和选择窗口。
- `Running`：物理更新进行中；仍允许紧急停止。
- `Paused`：保留选择和物理状态，但不移动窗口。
- 没有选择任何窗口时禁用“启动重力”。
- 启动后不允许悄悄把新出现的窗口加入控制列表。

### 7.3 托盘功能

MVP 若时间允许，加入系统托盘图标：

- 显示主界面
- 暂停/继续
- 停止并恢复
- 退出

托盘不是首个里程碑的阻塞项，但退出路径必须始终清晰。

## 8. 建议架构

建议解决方案结构：

```text
DesktopGravity.sln
src/
  DesktopGravity.App/          WPF UI、应用生命周期、依赖注入
  DesktopGravity.Core/         物理模型、规则、接口、配置模型
  DesktopGravity.Windows/      Win32 API、窗口枚举与移动实现
tests/
  DesktopGravity.Core.Tests/   物理与边界单元测试
  DesktopGravity.Windows.Tests/可测试的筛选规则与适配器测试
docs/
  architecture.md
  safety.md
```

建议职责划分：

- `IWindowCatalog`：枚举可控制窗口。
- `IWindowController`：读取和设置窗口位置。
- `IDisplayBoundsProvider`：提供显示器工作区。
- `IPhysicsEngine`：根据状态和时间步计算下一状态。
- `ISimulationCoordinator`：管理受控窗口与更新循环。
- `IHotkeyService`：注册和释放全局快捷键。
- `ISettingsStore`：读取、验证和保存本地配置。
- `WindowEligibilityPolicy`：集中判断窗口是否允许控制。

核心物理代码不得直接调用 Win32 API，以便在非 Windows CI 中测试数学与边界逻辑。

## 9. Win32 接口建议

根据实际需要最小化封装以下 API，不要一次引入不相关接口：

- `EnumWindows`
- `IsWindowVisible`
- `IsIconic`
- `GetWindowRect`
- `GetWindowTextLengthW`
- `GetWindowTextW`
- `GetWindowThreadProcessId`
- `GetClassNameW`
- `SetWindowPos`
- `MonitorFromWindow`
- `GetMonitorInfoW`
- `RegisterHotKey`
- `UnregisterHotKey`

移动窗口时优先采用不会激活、不会改变 Z 顺序的标志。P/Invoke 声明必须使用正确的字符集、句柄类型和错误处理，不得用裸整数代替所有 Win32 类型。

## 10. 更新循环与线程规则

- 物理计算与 UI 展示解耦。
- 使用单一协调器管理更新循环，避免每个窗口单独创建线程。
- 使用 `Stopwatch` 计算单调递增的时间差。
- 对异常过大的 `deltaTime` 进行截断，例如最大按 50 ms 处理。
- Win32 调用失败时只移除对应窗口，不中止整个模拟。
- UI 更新回到 WPF Dispatcher。
- 停止操作必须具备幂等性，连续调用多次也不会出错。
- 退出时取消循环、注销快捷键并释放资源。

## 11. 错误处理与日志

- 捕获单个窗口操作异常，记录错误类型、进程名和错误码。
- 默认不记录完整窗口标题，以减少敏感信息泄露。
- 日志文件大小应有限制，例如滚动保存最近 3 个文件、每个不超过 1 MB。
- 主界面只展示可理解的错误，例如“该窗口权限较高，已跳过”。
- 不向用户展示未处理的堆栈跟踪；开发模式可在日志中保留。
- 任一不可恢复错误必须触发安全停止，而不是继续移动窗口。

## 12. 测试要求

### 12.1 单元测试

至少覆盖：

- 不同 `deltaTime` 下的自由落体计算。
- 最大速度限制。
- 触底后的反弹速度。
- 小速度落地停止。
- 窗口不会穿过工作区底部。
- 初始位置已经越界时能够恢复。
- 重力为最小值、最大值和弹性为 0 时的行为。
- 显示器工作区变化后的边界修正。
- 配置缺字段、非法值和损坏 JSON 时回退默认值。
- 窗口筛选策略中的典型允许与拒绝案例。

### 12.2 手工验收矩阵

在真实 Windows 机器上测试：

- Windows 10 与 Windows 11（条件允许时）。
- 100%、125%、150% DPI。
- 单显示器和双显示器。
- 任务栏位于底部；其他位置作为后续兼容项。
- 记事本、文件资源管理器、Chrome/Edge、VS Code。
- 普通窗口、最大化窗口、最小化窗口。
- 拖动窗口过程中启停重力。
- 运行中拔掉副显示器。
- 快捷键冲突、程序退出和异常停止。

### 12.3 不可自动化的验证

Codex 必须明确列出需要用户在 Windows 桌面上观察的行为，不得声称仅凭单元测试已经验证真实窗口不会抖动、闪烁或被其他程序夺回位置。

## 13. MVP 验收标准

只有全部满足才算 MVP 完成：

1. 在受支持的 Windows 环境中可以构建并启动。
2. 主界面能稳定列出普通可见窗口。
3. 用户可以明确选择窗口，不会默认控制所有窗口。
4. 选中窗口能够下落、触底、反弹并最终静止。
5. 用户拖动受控窗口时程序不会明显争抢位置。
6. `Ctrl + Shift + G` 能在可感知的瞬间停止模拟。
7. “停止并恢复”不会把窗口放到屏幕外。
8. 系统窗口、高权限窗口和本程序窗口被安全跳过。
9. 任一窗口操作失败不会导致整个程序崩溃。
10. 核心物理测试、配置测试和筛选策略测试通过。
11. README 包含安装、使用、紧急停止、卸载、隐私和已知限制。
12. 不需要管理员权限，不联网，不收集遥测。

## 14. 后续版本规划

### V0.2：投掷与趣味模式

- 根据用户释放窗口前的位置变化估计水平和垂直速度。
- 支持窗口水平运动与左右边界碰撞。
- 月球、地球、木星预设。
- 零重力、反向重力和屏幕倾斜模式。

### V0.3：窗口间碰撞

- 使用轴对齐矩形（AABB）进行粗略碰撞。
- 位置修正与冲量分离，避免窗口互相嵌入。
- 限制同时参与碰撞的窗口数量，例如最多 12 个。
- 提供关闭碰撞的开关，性能不足时自动降级。

### V0.4：演示与创作者功能

- 一键“地震模式”。
- 鼠标点击位置产生短暂引力井。
- 黑洞吸引效果，但始终保留停止快捷键。
- 演示倒计时和参数预设。
- 可隐藏控制面板，方便录屏。

## 15. README 要求

README 至少包含：

- 一张 GIF 或短视频展示核心效果。
- 一句话简介和醒目的安全说明。
- 系统要求与安装步骤。
- 三步使用方法。
- 紧急停止快捷键。
- 功能清单与路线图。
- 隐私说明：本地运行、不上传窗口信息。
- 已知限制：管理员窗口、特殊窗口和全屏程序等。
- 完整卸载方法。
- 开源许可证。
- 贡献指南链接。

中文 README 为主，并提供 `README.en.md`。项目发布前补充截图或 GIF；不要在仓库中提交包含私人窗口标题的素材。

## 16. 实施顺序

Codex 应严格按以下阶段推进，每个阶段完成后运行对应测试：

### 阶段 0：仓库检查与方案确认

- 检查仓库现状和现有修改。
- 如果是空仓库，创建解决方案结构。
- 写下极简实现计划，但不要长时间停留在规划。

### 阶段 1：纯物理核心

- 创建窗口状态、边界和参数模型。
- 实现垂直重力、速度限制、反弹和静止。
- 编写并运行核心单元测试。

### 阶段 2：Windows 适配层

- 实现窗口枚举、筛选、位置读取和位置设置。
- 实现显示器工作区读取。
- 所有 Win32 失败都转换为可处理结果，不向核心层泄漏崩溃。

### 阶段 3：模拟协调器

- 连接物理核心与窗口控制器。
- 实现启动、暂停、停止、恢复和窗口消失处理。
- 确保更新循环可取消且停止幂等。

### 阶段 4：WPF 界面

- 实现窗口列表、选择、参数设置和状态显示。
- 实现按钮状态与输入验证。
- 不追求复杂视觉主题，先保证功能清楚。

### 阶段 5：安全机制

- 注册全局紧急停止快捷键。
- 完成退出清理、配置恢复和屏幕内位置修正。
- 验证程序不要求管理员权限。

### 阶段 6：交付质量

- 完善测试、README、许可证和构建说明。
- 运行格式检查、构建和测试。
- 输出手工 Windows 测试清单，不虚构未执行结果。

## 17. 给 Codex 的工作约束

- 这是“实现”任务，不是只写方案；在仓库中直接创建和修改所需文件。
- 先完成 MVP，不要擅自实现 V0.2 之后的功能。
- 不添加联网、AI、账号、遥测或自动更新功能。
- 不使用 DLL 注入、进程内存修改、键盘封锁或权限绕过。
- 不修改用户系统配置，不设置开机启动。
- 不执行破坏性 Git 操作，不覆盖仓库中无关的现有修改。
- 新增依赖前优先使用 .NET 内置能力；必须新增时说明理由。
- 对无法在当前环境验证的 Windows 行为明确标注为“待手工验证”。
- 每完成一个阶段，运行能运行的构建和测试并修复失败。
- 如果开发环境不是 Windows，仍应完成可移植核心和代码审查，但不得假装运行过 Windows 桌面集成测试。
- 最终答复必须包含：完成内容、关键文件、测试命令与结果、已知限制、Windows 手工验收步骤。

## 18. 可直接发送给 Codex 的启动指令

将本文件放在目标 Git 仓库根目录后，发送以下指令：

> 请完整阅读 `DesktopGravity_SPEC.md`，检查当前仓库，并按照其中的“实施顺序”实现 Desktop Gravity MVP。你已获授权在当前仓库内创建和修改代码、测试及文档，并运行非破坏性的构建与测试命令。不要实现规格中明确排除或列入 V0.2 之后的功能。优先保证紧急停止、窗口筛选、屏幕内边界和失败隔离。若当前环境无法运行 Windows GUI，请继续完成可验证的纯逻辑与项目结构，并准确列出需要我在 Windows 上手工验证的项目，不要声称已验证未实际运行的行为。

## 19. 完成定义

项目不是以“生成了很多代码”为完成标准，而是以以下结果为准：

- 一位普通用户能看懂如何选择窗口、开启重力和立即停止。
- 一次窗口控制失败不会影响其他窗口或导致应用崩溃。
- 关闭效果后，窗口仍然可见、可用、可恢复。
- 物理核心具有自动测试。
- 真实 Windows 环境中的未验证项被诚实、具体地列出。
- 仓库具备让其他人下载、构建、体验和贡献的基础文档。
